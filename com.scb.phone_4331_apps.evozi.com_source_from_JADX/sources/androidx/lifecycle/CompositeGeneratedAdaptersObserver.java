package androidx.lifecycle;

import p040o.Keep;
import p040o.createCount;
import p040o.entryRemoved;
import p040o.maxSize;
import p040o.setBackgroundDrawable$MediaBrowserCompat$ItemReceiver;

public class CompositeGeneratedAdaptersObserver implements createCount {
    private final Keep.read[] MediaBrowserCompat$ItemReceiver;

    public CompositeGeneratedAdaptersObserver(Keep.read[] readArr) {
        this.MediaBrowserCompat$ItemReceiver = readArr;
    }

    public final void MediaBrowserCompat$ItemReceiver(maxSize maxsize, entryRemoved.write write) {
        new setBackgroundDrawable$MediaBrowserCompat$ItemReceiver();
    }
}
