package p040o;

import android.content.SharedPreferences;
import android.content.res.Resources;
import java.util.ArrayList;
import java.util.List;

/* renamed from: o.isCompatIPv4Address */
public final class isCompatIPv4Address extends constrainedListMultimap {
    @HmlPinActivity
    public isCompatIPv4Address(Resources resources, SharedPreferences sharedPreferences) {
        super(resources, sharedPreferences);
    }

    public static List<getBaseAddress> MediaBrowserCompat$ItemReceiver(List<zzup> list) {
        ArrayList arrayList = new ArrayList();
        for (zzup next : list) {
            arrayList.add(new getBaseAddress(next.MediaBrowserCompat$CustomActionResultReceiver, getICheckDeserializerRtti.MediaBrowserCompat$CustomActionResultReceiver(next.MediaBrowserCompat$CustomActionResultReceiver, true), next.IconCompatParcelizer.MediaBrowserCompat$ItemReceiver, next.MediaBrowserCompat$ItemReceiver));
        }
        return arrayList;
    }
}
