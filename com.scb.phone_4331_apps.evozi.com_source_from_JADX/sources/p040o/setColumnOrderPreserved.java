package p040o;

import android.util.Log;
import java.io.File;
import java.io.IOException;

/* renamed from: o.setColumnOrderPreserved */
public final class setColumnOrderPreserved implements onContextItemSelected<setUseDefaultMargins> {
    public final /* synthetic */ boolean read(Object obj, File file, onActivityResult onactivityresult) {
        return IconCompatParcelizer((performOptionsMenuClosed) obj, file);
    }

    public final isRemoving IconCompatParcelizer(onActivityResult onactivityresult) {
        return isRemoving.SOURCE;
    }

    private static boolean IconCompatParcelizer(performOptionsMenuClosed<setUseDefaultMargins> performoptionsmenuclosed, File file) {
        try {
            stopLoading.MediaBrowserCompat$ItemReceiver(performoptionsmenuclosed.write().read.IconCompatParcelizer.MediaBrowserCompat$MediaItem.MediaBrowserCompat$CustomActionResultReceiver().asReadOnlyBuffer(), file);
            return true;
        } catch (IOException e) {
            if (Log.isLoggable("GifEncoder", 5)) {
                Log.w("GifEncoder", "Failed to encode GIF drawable data", e);
            }
            return false;
        }
    }
}
